import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { ClockDisplay } from './components/ClockDisplay';

ReactDOM.render(
  <ClockDisplay />, document.getElementById('root')
);
